wrap = "keep"
number = true
end_of_line = "lf"
validate = true
extensions = ["gfm", "mkdocs"]

exclude = [
  ".git/**",
  ".venv/**",
  "site/**",
]

[plugin.mkdocs]
align_semantic_breaks_in_lists = true
ignore_missing_references = true